async function refreshStorage(){const data=await fetchJSON('/api/health');const grid=document.getElementById('storage-grid');grid.innerHTML='';data.storage.forEach(s=>{const used=s.maxdisk?(s.disk/s.maxdisk)*100:0;grid.innerHTML+=`<article class='device-card'><div class='device-top'><h3>${s.storage}</h3><span class='led online'>${s.status||'OK'}</span></div><div class='mono'>${s.node}</div><div class='mini-gauge-row'>${gaugeHTML('USED',used)}</div><div class='muted'>${gb(s.disk)} / ${gb(s.maxdisk)}</div></article>`})}refreshStorage();setInterval(refreshStorage,5000);